import std
fn main(argc: i32, argv: &&i): i32 {
    if argc > 1 { 
        printf("> 1\n");
        if argc > 5 {
            printf("More than 5");
        };
        printf("is this cont4?");
        return 1000;
    };
    # if argc > 7 {
    #     return 10;
    # };
    return 0;
}